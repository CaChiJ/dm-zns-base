# Kernel module build artifacts
*.ko
*.o
*.mod
*.mod.c
*.mod.o
*.cmd
.*.cmd
.tmp_versions/
modules.order
Module.symvers

# Test run artifacts
# fio writes its verify state into the working directory unless a job passes
# --verify_state_save=0. Every suite does, but a hand-run fio still would not.
*-verify.state

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Local VM Settings
.vagrant/
.vagrant-home/

# Local Codex instructions and connection details
/AGENTS.md
